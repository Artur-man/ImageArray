% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zarrutils.R
\name{zarr-utils}
\alias{zarr-utils}
\alias{zarrcreateGroup}
\alias{open_zarr}
\title{zarrcreateGroup}
\usage{
zarrcreateGroup(store, name)

open_zarr(dir, name)
}
\arguments{
\item{store}{the location of (zarr) store}

\item{name}{name of the zarr store or group}

\item{dir}{directory/location of zarr store}
}
\value{
does not return anything, creates a zarr group in the store instead

does not return anything, opens a zarr store instead
}
\description{
get information of an ImgArray object
}
\section{Functions}{
\itemize{
\item \code{zarrcreateGroup()}: create zarr group

\item \code{open_zarr()}: open zarr stores

}}
\examples{
# zarr store path
dir.create(td <- tempfile())
zarr_name <- "test.zarr"
output_zarr <- file.path(td, zarr_name)

# open zarr store
open_zarr(dir = td, name = zarr_name)

# create group
zarrcreateGroup(store = output_zarr, name = "sample")
}
\concept{zarr-utils}
